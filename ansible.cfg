[defaults]
inventory = inventory/hosts.ini
roles_path = roles

# WARNING: Enable host_key_checking in production.
# Disabled for lab environment with dynamic IPs.
host_key_checking = False
timeout = 30
forks = 10

interpreter_python = auto_silent
vault_password_file = ~/.ansible/vault_pass
