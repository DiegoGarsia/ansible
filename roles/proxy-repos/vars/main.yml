---
# =============================================================================
# proxy-repos — переменные роли
# =============================================================================
# Этот файл автоматически подключается Ansible при использовании роли.
# OS-специфичные переменные (списки файлов) находятся в отдельных файлах
# (Ubuntu.yml, Debian.yml, RedHat.yml, default.yml) и подключаются из задач.
# =============================================================================
