---
# =============================================================================
# proxy-repos — переменные по умолчанию для ОС
# =============================================================================
# Этот файл подключается, если не найден файл ни для {{ ansible_distribution }},
# ни для {{ ansible_os_family }} в каталоге vars/. Здесь можно задать
# универсальные значения для неизвестных дистрибутивов.
# =============================================================================
