---
- name: Allow GitLab service ports in UFW
  community.general.ufw:
    rule: allow
    port: "{{ item }}"
    proto: tcp
  loop: "{{ gitlab_ufw_allowed_ports }}"

- name: Enable UFW
  community.general.ufw:
    state: enabled
    policy: deny
