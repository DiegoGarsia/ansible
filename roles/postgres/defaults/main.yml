---
postgres_packages:
  - postgresql
  - postgresql-contrib
  - python3-psycopg2

postgres_service: postgresql

postgres_db: testdb
postgres_user: testuser

postgres_password: "{{ vault_postgres_password }}"

postgres_ansible_tmp_dir: /var/lib/postgresql/.ansible/tmp
